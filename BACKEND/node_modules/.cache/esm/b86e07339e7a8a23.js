let express,Teacher;_9e7‍.x([["registerTeacherRoutes",()=>registerTeacherRoutes]]);_9e7‍.w("express",[["default",["express"],function(v){express=v}]]);_9e7‍.w("../../controllers/teacher",[["default",["Teacher"],function(v){Teacher=v}]]);

const router = express.Router();
const DEFAULT_VERSION = 'v1';

const teacherRoutes = () => {
    router.get('/', Teacher.getAllTeacher);
    return router;
}

       function registerTeacherRoutes(app) {
    app.use(
        `/api/${process.env.API_VERSION || DEFAULT_VERSION}/teachers`,
        teacherRoutes(),
    );
}

