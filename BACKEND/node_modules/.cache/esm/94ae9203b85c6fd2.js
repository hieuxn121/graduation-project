let express,User;_b85‍.x([["registerUserRoutes",()=>registerUserRoutes]]);_b85‍.w("express",[["default",["express"],function(v){express=v}]]);_b85‍.w("../../controllers/user",[["default",["User"],function(v){User=v}]]);

const router = express.Router();
const DEFAULT_VERSION = 'v1';

const userRoutes = () => {
    router.post('/', User.getUserInfo);
    router.post('/update', User.updateUserInfo);
    return router;
}

       function registerUserRoutes(app) {
    app.use(
        `/api/${process.env.API_VERSION || DEFAULT_VERSION}/user`,
        userRoutes(),
    );
}

