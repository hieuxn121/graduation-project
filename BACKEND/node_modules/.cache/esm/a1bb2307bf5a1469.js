let express,Category;_bf5‍.x([["registerCategoryRoutes",()=>registerCategoryRoutes]]);_bf5‍.w("express",[["default",["express"],function(v){express=v}]]);_bf5‍.w("../../controllers/category",[["default",["Category"],function(v){Category=v}]]);

const router = express.Router();
const DEFAULT_VERSION = 'v1';

const categoryRoutes = () => {
    router.get('/', Category.getAllCategory);
    return router;
}

       function registerCategoryRoutes(app) {
    app.use(
        `/api/${process.env.API_VERSION || DEFAULT_VERSION}/categories`,
        categoryRoutes(),
    );
}

